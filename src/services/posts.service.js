const posts = {
  principalPost: [],
  mostRecentPost: []
}


const principalPost = () => {
  posts.principalPost.map((post) => { post.user, post.image })
}

const mostRecentPost = () => {
  posts.mostRecentPost.map((post) => { post.user, post.image })
}